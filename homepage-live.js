import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';import{collection,getDocs,getFirestore,query,where}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';import{mergeScholarships}from'./scholarship-catalog.js';if(location.pathname.endsWith('/')||location.pathname.endsWith('/index.html')){const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));let remote=[];try{const snap=await getDocs(query(collection(db,'scholarships'),where('publishStatus','==','published')));remote=snap.docs.map(d=>({id:d.id,...d.data()}))}catch(e){console.warn('Homepage remote data unavailable; keeping verified catalog.',e)}const items=mergeScholarships(remote).filter(x=>x.publishStatus==='published').sort((a,b)=>(a.sortOrder??999)-(b.sortOrder??999));const featured=items.filter(x=>x.featured).slice(0,3);const chosen=featured.length>=3?featured:items.slice(0,3),cards=document.querySelector('.featured .cards');if(cards&&chosen.length)cards.innerHTML=chosen.map(s=>{const href=s.legacyUrl||`scholarship.html?slug=${encodeURIComponent(s.slug||s.id)}`;return `<article class="card"><div class="cover"><div><small>${esc(s.country||'دولي')}</small><h3>${esc(s.title||'منحة')}</h3></div></div><div class="body"><div class="pills"><span class="pill">${esc(s.statusLabel||'راجع الحالة')}</span><span class="pill gold">${esc(s.funding||'حسب البرنامج')}</span></div><p>${esc(s.shortDescription||'')}</p><div class="bottom"><div>الموعد النهائي<br><b>${esc(s.deadline||'حسب الجهة')}</b></div><a class="detail" href="${esc(href)}">التفاصيل</a></div></div></article>`}).join('');const stats=document.querySelectorAll('.stats .stat b'),countries=new Set(items.map(x=>x.country).filter(Boolean));if(stats[0])stats[0].textContent=items.length.toLocaleString('ar');if(stats[1])stats[1].textContent=countries.size.toLocaleString('ar');if(stats[2])stats[2].textContent='محدثة';}