import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';import{doc,getDoc,getFirestore}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';import{levelBadge}from'./level-badges.js';
const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),root=document.querySelector('#public-profile'),uid=new URLSearchParams(location.search).get('uid');const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
if(!uid){root.innerHTML='<div class="notice">رابط الملف غير صحيح.</div>'}else{try{const snap=await getDoc(doc(db,'publicProfiles',uid));if(!snap.exists()||snap.data().visible!==true){root.innerHTML='<div class="notice">هذا الملف غير متاح للعامة.</div>'}else{const p=snap.data(),level=Math.max(1,Math.min(10,Number(p.level)||1)),xp=Math.max(0,Number(p.xp)||0);root.innerHTML=`<div class="public-head"><div class="public-avatar">${esc((p.username||'ط').slice(0,1).toUpperCase())}</div><div><div class="public-name"><h1 style="margin:0">@${esc(p.username||'student')}</h1>${levelBadge(level,34)}</div><p class="muted">طالب في مجتمع شذرات</p></div></div><div class="public-stats"><div class="public-stat"><b>Level ${level}</b><small>المستوى</small></div><div class="public-stat"><b>${xp.toLocaleString('ar')}</b><small>XP</small></div><div class="public-stat"><b>${Array.isArray(p.badges)?p.badges.length:0}</b><small>الشارات</small></div></div><p class="privacy-note">يعرض هذا الملف المعلومات العامة فقط. البريد ورقم الجوال والطلبات والرسائل وبيانات الحساب الخاصة لا تظهر للطلاب الآخرين.</p>`}}catch(e){console.error(e);root.innerHTML='<div class="notice">تعذر تحميل الملف الآن.</div>'}}
