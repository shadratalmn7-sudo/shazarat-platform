import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';import{collection,getDocs,getFirestore,query,where}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function services(){const s=await getDocs(query(collection(db,'services'),where('publishStatus','==','published')));if(s.empty)return;document.querySelectorAll('[data-section] .grid').forEach(grid=>grid.replaceChildren());for(const d of s.docs){const x=d.data(),grid=document.querySelector(`[data-section="${x.category||'paid'}"] .grid`);if(!grid)continue;grid.insertAdjacentHTML('beforeend',`<article class="card ${x.category==='paid'?'paid':''}" data-type="${x.category}" data-live-service="${d.id}"><div class="icon">✦</div><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p>${x.category==='free'?'<div class="free">مجانية</div>':`<div class="price"><span class="amount">${Number(x.price||0)}$</span>${x.originalPrice?`<span class="old">${Number(x.originalPrice)}$</span>`:''}</div>`}<a class="button ${x.category==='free'?'free-action':'order'}" href="contact.html?type=service&service=${encodeURIComponent(x.title)}&serviceId=${encodeURIComponent(d.id)}">${x.category==='free'?'احجز استشارة':'اطلب الخدمة'}</a>${x.terms?`<div class="warning">${esc(x.terms)}</div>`:''}</article>`)}document.querySelectorAll('[data-section]').forEach(section=>{section.hidden=!section.querySelector('.grid')?.children.length})}
async function offers(){const s=await getDocs(query(collection(db,'offers'),where('publishStatus','==','published'))),grid=document.querySelector('main .grid');if(!grid||s.empty)return;grid.replaceChildren();for(const d of s.docs){const x=d.data();grid.insertAdjacentHTML('beforeend',`<article class="card paid" data-live-offer="${d.id}"><span class="tag">عرض</span><h2>${esc(x.title)}</h2><p class="muted">${esc(x.description||'')}</p><p>${x.originalPrice?`<span class="old">${Number(x.originalPrice)} دولار</span>`:''} <span class="price">${Number(x.price||0)} دولار</span></p>${x.terms?`<div class="notice">${esc(x.terms)}</div>`:''}<a class="btn primary" href="contact.html?type=service&service=${encodeURIComponent(x.title)}&offerId=${encodeURIComponent(d.id)}">اطلب العرض</a></article>`)}}
if(location.pathname.endsWith('services.html'))services().catch(console.error);if(location.pathname.endsWith('offers.html'))offers().catch(console.error);
