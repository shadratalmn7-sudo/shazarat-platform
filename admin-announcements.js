import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import{addDoc,collection,getDocs,getFirestore,orderBy,query,serverTimestamp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
import{firebaseConfig}from'./firebase-config.js';
const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),main=document.querySelector('.admin-main');
main.innerHTML=`<div class="admin-title"><div><span class="eyebrow">الإشعارات</span><h1>التنبيهات والإعلانات</h1></div></div><div class="admin-live-note"><b>إعلان عام:</b> يظهر للطلاب عند دخول الموقع. استخدمه للمواعيد المهمة، إعلان منحة، خدمة جديدة أو تنبيه عاجل.</div><div class="admin-workspace"><section class="admin-form-panel"><form id="announcement-form"><div class="admin-form-grid"><label>العنوان<input name="title" required minlength="3"></label><label>الحالة<select name="publishStatus"><option value="published">نشر الآن</option><option value="draft">مسودة</option></select></label><label class="wide">نص الإعلان<textarea name="body" required minlength="3" maxlength="2000"></textarea></label></div><p class="admin-status"></p><div class="admin-form-actions"><button class="btn primary">حفظ الإعلان</button></div></form></section><div class="desktop-table-wrap"><table class="live-admin-table"><thead><tr><th>العنوان</th><th>النص</th><th>الحالة</th><th>التاريخ</th></tr></thead><tbody id="ann-body"></tbody></table></div></div>`;
const form=document.querySelector('#announcement-form'),body=document.querySelector('#ann-body'),status=form.querySelector('.admin-status'),esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){const s=await getDocs(query(collection(db,'announcements'),orderBy('createdAt','desc')));body.innerHTML=s.empty?'<tr><td colspan="4">لا توجد إعلانات.</td></tr>':s.docs.map(d=>{const x=d.data();return`<tr><td><b>${esc(x.title)}</b></td><td>${esc((x.body||'').slice(0,120))}</td><td>${esc(x.publishStatus)}</td><td>${x.createdAt?.toDate?x.createdAt.toDate().toLocaleString('ar-SA'):'—'}</td></tr>`}).join('')}
form.onsubmit=async e=>{e.preventDefault();status.textContent='جارٍ الحفظ…';try{await addDoc(collection(db,'announcements'),{title:form.title.value.trim(),body:form.body.value.trim(),publishStatus:form.publishStatus.value,createdAt:serverTimestamp(),updatedAt:serverTimestamp()});status.textContent=form.publishStatus.value==='published'?'تم النشر وسيظهر للطلاب عند دخول الموقع.':'تم حفظ المسودة.';form.reset();await load()}catch(err){console.error(err);status.textContent='تعذر الحفظ. تحقق من صلاحيات المالك.'}};load().catch(console.error);
