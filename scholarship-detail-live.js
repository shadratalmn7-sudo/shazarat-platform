import { getApp, getApps, initializeApp } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import { doc, getDoc, getFirestore } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
import { firebaseConfig } from './firebase-config.js';
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),list=(title,items)=>Array.isArray(items)&&items.length?`<section class="box"><h2>${title}</h2><ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section>`:'';
const main=document.querySelector('#dynamic-scholarship'),slug=new URLSearchParams(location.search).get('slug');
try{if(!slug)throw new Error('missing');const app=getApps().length?getApp():initializeApp(firebaseConfig),snap=await getDoc(doc(getFirestore(app),'scholarships',slug));if(!snap.exists())throw new Error('missing');const s=snap.data();document.title=`${s.title} | شذرات`;main.innerHTML=`<header class="detail-head dynamic-detail"><div class="container"><a class="back" href="scholarships.html">← جميع المنح</a><div class="detail-emblem">${esc((s.country||'✦').slice(0,2))}</div><p class="detail-provider">${esc(s.provider||'منحة دراسية')}</p><h1>${esc(s.title)}</h1><p>${esc(s.shortDescription)}</p><div class="chips"><span>${esc(s.country||'دولي')}</span><span>${esc(s.funding||'راجع تفاصيل التمويل')}</span>${(s.studyLevels||[]).map(x=>`<span>${esc(x)}</span>`).join('')}<span>${esc(s.statusLabel||'راجع الموعد الرسمي')}</span></div></div></header><div class="container layout"><div><section class="box warning"><h2>المواعيد والحالة</h2><div class="date-grid"><div><small>فتح التقديم</small><b>${esc(s.openDate||'بانتظار الإعلان')}</b></div><div><small>الموعد النهائي</small><b>${esc(s.deadline||'حسب الجهة الرسمية')}</b></div></div></section>${s.overview?`<section class="box"><h2>نبذة عن المنحة</h2><p>${esc(s.overview)}</p></section>`:''}${list('شروط الأهلية',s.eligibility)}${list('المستندات المطلوبة',s.documents)}${Array.isArray(s.steps)&&s.steps.length?`<section class="box"><h2>خطوات التقديم</h2><div class="timeline">${s.steps.map((x,i)=>`<div class="step"><b>${i+1}</b><span>${esc(x)}</span></div>`).join('')}</div></section>`:''}</div><aside class="box side"><h2>روابط موثوقة</h2><p class="muted">المصدر الرسمي هو المرجع النهائي لأي تحديث.</p>${s.officialUrl?`<a class="official" href="${esc(s.officialUrl)}" target="_blank" rel="noopener">المصدر الرسمي ↗</a>`:''}${s.applyUrl?`<a class="official secondary" href="${esc(s.applyUrl)}" target="_blank" rel="noopener">التقديم الرسمي ↗</a>`:''}</aside></div>`}catch(e){main.innerHTML='<section class="detail-loading"><h1>لم نجد هذه المنحة</h1><p>قد تكون مخفية أو أن الرابط غير صحيح.</p><a class="official" href="scholarships.html">العودة إلى جميع المنح</a></section>'}
