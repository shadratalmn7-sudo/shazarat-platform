const currentPage=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('nav a,.menu-links a,.drawer-links a').forEach(link=>{const href=(link.getAttribute('href')||'').split('#')[0];if(href===currentPage||(currentPage==='index.html'&&(href===''||href==='index.html'))){link.classList.add('is-current');link.setAttribute('aria-current','page')}});document.querySelectorAll('.hero,.page-hero').forEach(hero=>{if(!hero.querySelector('.ambient-shapes')){const shapes=document.createElement('div');shapes.className='ambient-shapes';shapes.innerHTML='<span></span><span></span><span></span>';hero.prepend(shapes)}});document.querySelectorAll('.journey-tab').forEach(tab=>tab.addEventListener('click',()=>{const id=tab.dataset.panel;document.querySelectorAll('.journey-tab,.journey-panel').forEach(el=>el.classList.remove('active'));tab.classList.add('active');document.getElementById(id)?.classList.add('active')}));const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced){const items=document.querySelectorAll('.card,.box,.stage,.stat,.service,.notice,.page-hero h1,.page-hero .lead,.hero h1,.hero-grid>*,.interactive-zone .container');items.forEach((el,i)=>{el.classList.add('reveal');el.style.transitionDelay=`${Math.min(i%4,3)*70}ms`});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in-view');observer.unobserve(entry.target)}}),{threshold:.1,rootMargin:'0px 0px -25px'});items.forEach(el=>observer.observe(el));const glow=document.createElement('div');glow.className='cursor-glow';document.body.appendChild(glow);document.addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});document.querySelectorAll('.journey-stage,.dashboard').forEach(card=>{card.classList.add('tilt');card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*3}deg) rotateY(${x*4}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}
