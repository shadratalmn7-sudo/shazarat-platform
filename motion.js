if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const items=document.querySelectorAll('.card,.box,.stage,.stat,.service,.notice,.page-hero h1,.page-hero .lead,.hero h1,.hero-grid>*');items.forEach((el,i)=>{el.classList.add('reveal');el.style.transitionDelay=`${Math.min(i%4,3)*70}ms`});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in-view');observer.unobserve(entry.target)}}),{threshold:.1,rootMargin:'0px 0px -25px'});items.forEach(el=>observer.observe(el))}
