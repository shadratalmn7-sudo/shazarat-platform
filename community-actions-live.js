import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';import{getAuth}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js';import{addDoc,collection,deleteDoc,doc,getDoc,getFirestore,serverTimestamp,setDoc}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';const app=getApps().length?getApp():initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app);const requireUser=()=>auth.currentUser||null;async function toggleDoc(ref,data){const s=await getDoc(ref);if(s.exists()){await deleteDoc(ref);return false}await setDoc(ref,data);return true}document.addEventListener('click',async e=>{const button=e.target.closest('button');if(!button)return;const post=button.closest('.community-post'),comment=button.closest('.comment-item');const label=button.textContent.trim();if(!post||!['مفيد','حفظ','إبلاغ'].some(x=>label.startsWith(x)))return;e.preventDefault();e.stopImmediatePropagation();const user=requireUser();if(!user){document.querySelector('#login-dialog')?.classList.add('is-open');return}const postId=post.dataset.postId;button.disabled=true;try{if(label.startsWith('مفيد')){const ref=comment?doc(db,'communityPosts',postId,'comments',comment.dataset.commentId,'helpful',user.uid):doc(db,'communityPosts',postId,'helpful',user.uid);const active=await toggleDoc(ref,{uid:user.uid,createdAt:serverTimestamp()});button.textContent=active?'مفيد ✓':'مفيد';button.setAttribute('aria-pressed',String(active));}else if(label.startsWith('حفظ')){const ref=doc(db,'savedCommunityPosts',`${user.uid}_${postId}`);const active=await toggleDoc(ref,{userId:user.uid,postId,createdAt:serverTimestamp()});button.textContent=active?'محفوظ ✓':'حفظ';button.setAttribute('aria-pressed',String(active));}else if(label.startsWith('إبلاغ')){const reason=prompt('اكتب سبب البلاغ باختصار (5 أحرف على الأقل):')?.trim();if(!reason||reason.length<5)return;await addDoc(collection(db,'communityReports'),{reporterId:user.uid,postId,commentId:comment?.dataset.commentId||null,reason:reason.slice(0,500),status:'open',createdAt:serverTimestamp()});button.textContent='تم الإبلاغ ✓';button.disabled=true;return}}catch(err){console.error(err);button.title=err?.code==='permission-denied'?'يلزم نشر قواعد Firebase الجديدة أو إكمال الحساب.':'تعذر تنفيذ العملية الآن.'}finally{if(button.textContent!=='تم الإبلاغ ✓')button.disabled=false}},true);